# SESSION 2026-06-09 — Tier B 准备 + 库清洗

## 背景
继续 6-08 收尾的 Tier B（付费墙取全文）。开场先核对库实际状态（与 6-08 交接一致：40 篇/30 已总结/13 引用边）。

## 做了什么
1. **环境核查**：opencli v1.8.3，`doctor` 全绿（daemon:19825 / 扩展 v1.0.19 / profile `8fnbkdfj` 在线）。config 的 `tier_b.proxy_login_template` 仍是占位。
2. **库清洗（删 6 篇垃圾/跑题）**——用户拍板全删：
   - #29 `10.38124/ijisrt/...`（IJISRT 掠夺性水刊，相关性被标题骗到 55）
   - #36 `10.1016/j.tics.2020.09.002`（AI 与动物常识，跑题）
   - #33 `10.1007/s10055-022-00644-4`（VR 社交态度检测，跑题）
   - #38 `10.1109/jrfid...`（虚拟驾驶员认知 RL，发在 RFID 刊，错配）
   - #32 `10.1016/j.simpat.2014.06.005`（2014 行人群体仿真，旧+偏题）
   - #39 `10.1186/s10033...`（下肢外骨骼增强，擦边）
   - 操作：事务内删 citations(牵连0)/paper_topic(6)/papers(6)，按 relevance desc 重算 rank（34 篇）。
3. **免费捞 recover_oa**：剩 4 篇全失败（ASE/AMP unpaywall 403；Walk This Way/#17 无免费源）→ 全部确认走 Tier B。

## 现状
- **34 篇**：30 summarized / 3 pdf_failed（ASE,AMP,Walk This Way）/ 1 discovered（#17 plrev 物理角色动画综述）。
- 待 Tier B：上述 4 篇。

## Tier B 设计（已与用户确认，待实现）
- **全自动**：opencli 用常驻 profile `8fnbkdfj` 开代理 URL → 探到登录/Duo 页 → Telegram `notify` 喊用户过 Duo → `waitForReply("go")` → 会话活着时一口气抓完 → 结束 `notify` 报告。
- **取 PDF 不碰 cookie**（httpOnly）：让 Chrome 自己下。两路在 ASE 实测二选一：① `browser network --detail <key> --raw` 抠响应体；② `browser wait download` 让 Chrome 下到下载目录再搬。
- 跑通后固化为 `pipeline/fetch_tierb.js`，复用 slug+pdftotext+落库，自带日志（run.log + logs/tierb-<date>.log）。

## 卡点 / 下一步
- **等用户给真实代理 URL**：在登录好的 Chrome 打开 ASE（`https://doi.org/10.1145/3528223.3530110`），复制地址栏最终 URL → 反推 NYU 代理改写格式 → 填 config → 在 ASE 上实抠定方案。
- 之后：放大到 200 前补“文章评价/筛选体系”（来源质量信号挡掉掠夺刊）。
