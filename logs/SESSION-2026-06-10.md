# SESSION 2026-06-10 — 质量评价体系（硬信号+标记优先）+ Codex 跨模型评审团

> 本文件记录"评价体系"这条会话线的工作。同日另一个 Claude 实例在跑 `rl-general-toolbox` 主题（见 run.log），如有它的操作记录可往下追加。

## 这次会话干了什么（按时间序）

### ① 硬信号质量体系上线（lib/quality.py + config/quality/）
- 抓了 Beall's 衍生黑名单（stop-predatory-journals）：**1309 期刊 + 1161 出版商**，加工成规范化 txt。
- **发现 IJISRT 不在 Beall's 名单里**（名单 2017 停更）→ 建 `local_blocklist.txt`（手工追加名单）+ `doi_prefix_blocklist.txt`（IJISRT=10.38124，DOI 前缀挡法改名躲不掉）+ `venue_whitelist.txt`（本领域 38 个顶会顶刊，免疫误杀）。
- OpenAlex 字段接入：`sources.py` 补采 `is_retracted`/`is_in_doaj`/`publisher`，merge.py 跟进。
- 修了一个误标：OpenAlex 常把已发表论文（如 SIGGRAPH 的 PDP）的 venue 标成 arXiv → 规则改为"有正式 DOI（非 10.48550）的不算预印本"，flag 从 46 降到 34。

### ② 设计转向：用户拍板"标记优先，不一刀切 ban"
> 核心论点（用户认可）：**污染不发生在存进去，发生在用的时候忘了它是什么。**
- **block 只留死刑信号**：撤稿 + DOI 前缀黑名单。
- **名单命中降为 suspect**：照常入库，但标记持久化到 `papers.quality_tier/quality_signals`（新列，自动迁移），每个下游出口认标记：
  - `summarize_auto`：suspect → prompt 自动注入**质疑模式**（开头警示行、结果写"作者声称"、≥5 条质疑、主动找硬伤）；flag → 注一句"未经同行评审"。
  - `render_topic`：表格 ⚠️ + 单独"低可信来源"节。
  - 未来 RAG：检索出口必须过滤/降权 suspect（已写进 CLAUDE.md 待办 #6 硬约束）。
- commit 闸门：block 拒；suspect/flag 需 relevance≥45（config `quality.flag_min_relevance`）。
- `audit_quality.py`：回溯审计 + verdict 回写 DB；`--apply` 只删 block。**对 129 篇实跑：block=0 / suspect=0 / flag=34（全是真预印本）/ trusted=36 / ok=59**，标记已落库。

### ③ Codex 跨模型评审团上线
- 用户装好 codex CLI 并 `codex login`（ChatGPT 订阅，零 API 费）。`codex-cli 0.139.0`。
- `lib/codex.py`：`codex exec --output-last-message` 取干净输出，镜像 lib/claude.py，pool 复用。
- **打分魔鬼代言人**（score_auto，config `quality.codex_panel`，**默认 false 还没开**）：Codex 同批找"该拒"理由 → `panel_objection` 写进 scores/batch_*.json；commit 合议=**边界分(<60)+异议→挡；高分+异议→入库但异议追进 relevance_reason。Codex 无否决权。**
  - 试金石 3 篇全对：放过 DeepMimic，咬掉土木"钢结构加固"（关键词碰瓷）和 metaverse 水文。
- **总结核查** `verify_summaries.py <id> [pct] [并发] [--limit N]`：suspect 必核 + 抽 10%，Codex 对照原文核数字/论断，只出报告 → `topics/<id>/summary_verification.md`。
  - **首测 2 篇即抓 1 个 major 幻觉**：总结把"行为变化未被预先指定"写成"面对训练中未见的下坡"（原文域随机化含斜坡）。另 1 篇 minor（H3/H4 张冠李戴、量表来源说错）。
  - 修了一类误报：注入的元信息行（引用数/DOI）在 prompt 里豁免。

### ④ 顺带
- 上午确认 `fetch_tierb` Python 版首次端到端实跑通过（4/4 抓到，待办 #1 关闭，是另一实例的 run 触发的）。
- `score_auto` 的打分 prompt 现在带 venue。

## 明天可以接着做的（等用户决定）
1. **要不要开 `quality.codex_panel`**（config.json 改 true）——建议下轮跑新主题时开，打分级很便宜。
2. **要不要对 129 篇跑完整 10% 抽检**（13 篇，约十几分钟）——2 篇就有 1 个 major，幻觉率值得量化；数据出来再定抽检比例要不要调高。
3. 所有改动**未 commit**（另一实例还在跑 + 改过 fetch_tierb.py，等跑完一起提交）。
4. 远期照旧：放大到 200（现在有挡板了）、FTS5+ask.py（RAG 第一步，记得 quality_tier 过滤）。

## 测试记录
- quality.assess 单元 8 用例全过（含白名单免疫、DOAJ、撤稿、前缀）。
- commit 闸门临时库 E2E：block 拒 / suspect 高分入库带标记 / suspect 低分挡 / 老格式 candidates 兼容。
- suspect 全链路临时库 E2E：commit→papers 列→worklist→质疑 prompt→render ⚠️/低可信节。
- panel 合议临时库 E2E：border+异议挡、高分+异议入库带 [Codex异议]。
- verify 真实库 --limit 2 实跑（结果见上）。
