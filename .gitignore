# 大体积 / 版权全文 —— 绝不入库或推送
store/pdfs/
store/text/
store/dl_tmp/

# sqlite 旁路文件（主 .sqlite 入库）
db/*.sqlite-wal
db/*.sqlite-shm

# 可重建的运行中间产物
topics/*/candidates.json
topics/*/scores/
topics/*/summarize_worklist.json
topics/*/selected.json
store/update_worklist.json
store/cross_topic.json

# 密钥（绝不入库）
config/telegram.json
config/x11vnc.pass
config/x11vnc.plain

# 详细/可重建日志（run.log 入库，详细日志不入库）
logs/pipeline-*.log
logs/tierb-*.log
# bot 运行时文件（pid/inbox/会话/日志）
logs/bot*
logs/x11vnc.log
logs/websockify.log

# 第三方参考库（体积大，不入库）
ref/

# 远程看屏用的 noVNC 静态文件（第三方,可重新下载,不入库）
vendor/

# 杂项
node_modules/
__pycache__/
*.pyc
.DS_Store
db/fts.sqlite

# 知识库检索升级(2026-06-18):嵌入模型缓存(~1.2GB,可重下,在检索模块内) + 坐标索引(可重建)
# README 保留以说明用途,模型本体不入库
pipeline/retrieve/models/*
!pipeline/retrieve/models/README.txt
db/vec.sqlite
topics/*/literature_corpus.yaml
store/literature_corpus_all.yaml

# 本地备份/临时(2026-06-17):重做前的安全快照 + cron 运行日志,不入库
logs/redo-*/
logs/wipe-*/
logs/cron-*.log
logs/cron-smoke.sh
