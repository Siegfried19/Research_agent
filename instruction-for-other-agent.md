# 怎么调查这个知识库（给来查的 agent 看）

> 本文件是"怎么调查这个知识库"的**单一真相源**（随库走，改一处所有来查的 agent 下次读到的就是新版）。
> 各消费端只需在自己的记忆 / `CLAUDE.md` 里留一条"**库在哪 + 先读这份**"的短指针，**别把下面的纪律抄进去**（会漂），用时现读本文件即可。（v1 草案，用户可改。）

## 你是谁、要干什么
你是被叫来在这个论文/资料库里**做调查**的 agent。**调查循环归你拥有**——没有谁替你跑
问答管道。你自己看地图、自己决定读什么、自己得出有据的结论。库经 SSH 挂载在本机，
**全是本地文件**，直接 Read 即可。

## 第一步：拿最新地图
```
python3 pipeline/retrieve/map.py
```
纯 stdlib，任意 python3 都能跑（不用 conda）。它现拼一张**当前**库地图（也落一份
`data-base/INDEX.md`）：按 主题 → facet 分组，每篇一条 = 标题/年/venue/标记 + 一行家目录
(`storage/sources/<slug>/`)和里面的文件 + id。地图是**目录**，不是答案——据它决定去读哪些。

## 目录布局（你要读的料）
- 每篇一个家：`storage/sources/<slug>/`
  - `vN.md` —— 中文结构化总结（N 最大的是最新版；地图已给你最新版路径）
  - `paper.pdf` —— 论文原件 ／ `source.md` —— web 源正文
- 读总结还是读原件、读多深，**你自己判断**（见下纪律）。

## 地图上的标记是什么意思
- `预印本(未评审)` —— 未经同行评审，权重/措辞自己拿捏。
- `掠夺刊嫌疑` —— 出处存疑，结论需格外核实。
- `〔网络源〕` —— 博客/工具文，不是论文。
- `重大存疑` —— 跨模型核查疑总结写错（方向反转/张冠李戴/过度声称），**引用前务必回原件核**。
- `小瑕疵` —— 核查见轻微问题（措辞略强/孤立论断），基本可信。
- `未充分核查` —— 当前版总结未经/未充分交叉核验（含从没核、新版没跟上、有数字没核到），别全信、要紧处下钻原件。
- 没标记 = 已核通过（pass）。

## 调查纪律

**1. 召回优先——先尽量不漏，再谈精炼。**
- 地图列的是**全库**。按**你自己的问题**判断每篇相不相关，**别按它归在哪个主题来判**——很多论文跨主题、内容交叉，一篇归在「RL」下的，可能正好答你关于「agent 编排」的问题。
- 别因为"这一段标题看着没关系"就**整段跳过**；列表长时系统地扫，当心中段看漏。
- **漏掉一篇相关的是最严重的失败**——后面读得再细也补不回。宁可多扫几篇，别少看。

**2. 质量优先，别为省 token 走捷径。**
- 看着相关的总结**都读**，该深读就深读。你是被叫来认真调查的，不是扫一眼交差的。
- **别拿地图/标题当答案**——结论必须落在你**真读过**的总结/原件上；地图只是定位用的目录。

**3. 总结是主力，精确论断下钻原件。**
- 中文总结（`vN.md`）是 curated 的，多数问题读总结就够。
- 但总结**有意略去精确数字**：要**具体数字 / 原话 / 关键定量论断**时，打开原件（`paper.pdf` / `source.md`）**核一手**——别把总结里的数字当精确值引，也别凭一句蒸馏论断就下关键结论。

**4. 认标记，别无视**（标记含义见上一节）。
- `重大存疑`：**引用前务必回原件核**，别直接信。
- `预印本(未评审)` / `掠夺刊嫌疑` / `〔网络源〕`：可信度打折，引用时**显式说明**出处性质。
- `未充分核查`：当前版没充分交叉核验，要紧处下钻原件。

**5. 闭集引用，绝不编造。**
- 只引**库里真实存在**的来源，用地图给的 `id`（DOI/arxiv/url）作句柄。
- **绝不**编造库外来源或 DOI。万一要用库外常识补充，**显式标出"这不是库里的"**，别和库内来源混。

**6. 撑不起就老实说。**
- 库里材料不足以支撑结论，就直说**"库里没有相关内容" / "不足以判断"**——别用库外知识硬凑一个像样答案。诚实的"不知道"比编出来的"知道"有用。

**7. 结果要有据、带线索。**
- 结论**逐条带来源**（`id` + 路径）——不管是你自己后面要深挖、还是上游 agent 接着用，都能顺线下钻原件；蒸馏要点、别倒原文。
- （若你是被上游叫来专门查的，把"结论 + 线索"交回去，深读留给有上下文的它。）
