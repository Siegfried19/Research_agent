---
paper_id: 10.1109/tpami.2022.3223407
version: 3
based_on: []
created_at: 2026-06-17
note: 事实核查修正(Codex 抽检发现 3 处问题)
---

# Reward-Adaptive Reinforcement Learning: Dynamic Policy Gradient Optimization for Bipedal Locomotion
> Changxin Huang, Guangrun Wang, Zhibo Zhou, Ronghui Zhang, Liang Lin · 2022 · IEEE Transactions on Pattern Analysis and Machine Intelligence · 引用 38 · DOI 10.1109/tpami.2022.3223407

## 一句话
针对双足行走这种"多准则奖励"任务,本文不把各分量奖励求和成单一价值,而是用**多头 critic** 给每个奖励分量学一套独立价值函数(产生"混合策略梯度"),再用一组随训练动态变化的**优先级权重**让机器人"先学平衡、后学前进",在扰动/障碍/斜坡三类任务及 MuJoCo 上都优于求和式基线 DDPG,并迁移到了实体机器人。

## 解决了什么问题
双足行走奖励通常由多个分量构成(向前、步态、能耗、姿态等),"Most existing approaches simply add up the component rewards to learn a single value function [5], [10], which may break the correlation between different rewards and therefore limit learning efficiency [11], [12]"(p.1 §I)。即求和会丢掉分量之间的依赖信息;作者举例说求和后单一 Q 值无法指明是哪个因素拖累了策略——"when the robot receives a negative reward, it cannot understand whether it is due to the unnatural pose or the over-torque of servomotors"(p.3 §IV-A),从而学习效率受限、策略次优。

## 用什么方法解决的
两件事:(1)多头 critic——"The proposed method applies a multi-head critic to learn a separate value function for each reward component. This leads to hybrid policy gradient"(p.1 Abstract);(2)动态权重——"We further propose dynamic weight, allowing each component to optimize the policy with different priorities. This hybrid and dynamic policy gradient (HDPG) design makes the agent learn more efficiently"(p.1 Abstract)。整套构建在 DDPG 之上以支持连续动作。

---

## 动机 & 背景
传统双足控制(ZMP、HZD、DCM)依赖已知动力学/运动学——"such manually designed policies heavily based on the precondition that the robot dynamics and kinematic are known and determined. They cannot be implemented on robots that dynamics and kinematic are unknown"(p.2 §II),且"it is challenging to handle difficult situations where obstacles and inclined surfaces are present"(p.2 §II);模仿学习又需要人工设计专家策略——"these methods rely on human experts to manually design a control policy as reference, which can be laborious"(p.2 §II)。RL 路线虽能在未知动力学下学策略,但既有 RL 工作多用奖励求和。作者指出其方法承接 HRA / drQ 的"奖励分解"思想,但关键区别在动作空间:"However, both HRA and drQ are based on Q-learning and can only be used for discrete action space tasks. Our HDPG is based on DDPG [13], which allows learning continuous actions"(p.2 §II)——这正是把分解奖励思想搬到连续控制双足上的动机。

## 方法细节
**多头 critic。** 奖励被分解成向量,"Each head of the critic learns an action-value corresponding to a specific sub-reward"(p.3 §IV-A 公式(4)),整体 Q 值定义为各头之和 Q(s_t,a_t)=Σ_{k=1}^K Q_k(s_t,a_t)。critic 损失(式(7))为各头 TD 误差平方之和 L(θ^Q)=E·Σ_{k=1}^K[(y_{t,k}−Q_k(s_t,a_t|θ_k^Q))²],其中 y_{t,k}=r_{t,k}+γQ_k(s_{t+1},π(s_{t+1})|θ_k^Q)。网络结构上各头不是完全独立:"Only the last layer is independent of each other"(p.3 §IV-A 公式(7)),即共享底层、仅末层分头。

**混合 + 动态策略梯度。** 朴素情形下所有 Q 值以相同权重更新策略(式(8) ∇J≈Σ_{k=1}^K ∇J_k)。本文的核心是给每个分量梯度一个动态优先级权重 m_k,总梯度 ∇J=Σ m_k∇J_k。直觉是:"Intuitively, the motivation of dynamic weights is to encourage the agent to learn the easier components first, and then gradually learn the more complex components"(p.4 §IV-B),即先学平衡、后学前进。权重由当前策略下各分量 N 个奖励样本的均值 μ_k 与方差 σ_k 决定(式(9) m_k = K(μ_k+e^{σ_k²}) / Σ_k(μ_k+e^{σ_k²})):某分量"is easier to get a larger reward for the current policy, so it is easier to learn for the current policy, and vice versa"(p.4 §IV-B),而方差大表示该分量上策略更不稳定、应提高其学习率。权重不是每步更新——"the priority weights will be updated every T episodes"(p.4 §IV-B)。

**奖励设计(6 个子奖励)。** "expressed as step reward, gait reward, height reward, torque reward, orientation reward and fall down reward"(p.4 §IV-C)。其中步态奖励 r^g=w_1 d^g cosθ^g(仅步态发生时,否则 0,式(10))、步进奖励 r^s=w_2 d^s cosθ^s(式(11))、力矩奖励 r^f=−w_3 Σ|f^i|(式(12)):"The torque reward is inverse proportional to the torque magnitude of the joint motors. Negative torque reward is to punish excessive torque of servomotors while walking"(p.4 §IV-C);高度奖励 r^h=−w_4|h_t−h_0|(式(13))、姿态奖励 r^o=−w_5(|α^r|+|α^p|)(roll、pitch,式(14));跌倒奖励为固定大负值——"the fall down reward r d t is set as -50 for the robot fell to the ground"(p.4 §IV-C 式(14)下方)。

## 数据集 & 实验设置
**机器人/仿真。** 自建 AIDA 机器人:"The robot contains 10 degrees of freedom (DoF), and each leg has 5 DoF"(p.5 §V),ROS+Gazebo(ODE 物理引擎)、SolidWorks 建模,质心维持在骨盆中心。状态含时序:"The combined states yield 69-dimensional state space (23D for each state). The predicted action is the angular position of each joint, which is a 10-dimensional vector"(p.5 §V-C)。actor 为 3 层全连接——"The actor network consists of 3 fully connected layers",三层尺寸 (69,128)、(128,256)、(256,10)(p.5 §V-C);critic 把动作经 (10,128)、状态经 (69,128) 各过一层后拼接,再过两层 (256,256) 与 (256,6) "produce predicted 6 Q-values, each value corresponding one reward component"(p.5 §V-C)。

**训练超参。** "the discount factor is set as 0.99. The learning rate of actor network and critic network are set as 1e−5 and 1e−4, respectively. And the training batch size is set as 64"(p.5 §VI),优先级权重每 T=20 episodes 更新一次,Adam。奖励权重:"where w1 and w3 are set as 1.0 and 0.001, respectively. The other weights (e.g., w2, w4, w5) are set as 1.0. We use simple max-min normalization for each component"(p.5 §VI),归一化到 (0,1)。

**基准任务。** 三类双足任务:"walking under random disturbances, walking cross obstacle, and walking on the slope"(p.5 §V-B)——扰动 6N–14N、持续 0.2s、含前/后/侧向;障碍高度 0.6–1.4cm(图6图注 "height from range (0.6, 1.4)(cm)");斜坡角 3–15 度(图6图注 "angle from range (3, 15)(deg)")。**抗扰动任务**每个测试做 100 次试验——"We conducted 100 trials for each test"(图5图注);障碍与斜坡任务原文只说"成功=稳定行走 10 秒"(图6图注),未给出试验次数。对比对象为 ZMP(传统)、DDPG(单价值基线)、MHDDPG(只有多头、权重静态 m=1)、HDPG(完整版)。另在 MuJoCo Hopper/Walker2d/HalfCheetah-v3 上验证泛化。

## 主要结果 & 结论
**学习效率(消融出处:与去掉动态权重的 MHDDPG、求和式 DDPG 对比)。** "The accumulative reward of HDPG starts to rise after training 2k episodes while MHDDPG and DDPG require 4k and 6k episodes, respectively"(p.6 §VI-A 图4)——HDPG 起步最快、最终奖励最高。

**抗扰动(图5)。** 6N 时三种 RL 都好;到 8N 侧向时"only HDPG can maintain a success rate of over 90%. In comparison, the success rate of DDPG and MHDDPG dropped sharply to about 80%"(p.6 §VI-A1);到 10N 时传统方法明显崩"the success rate of ZMP in 3 directions is only 38%, 38% and 24% respectively. While the other RL-based methods can maintain a success rate of over 50%"(p.6 §VI-A1)。总体上"the proposed MHDDPG and HDPG outperform DDPG on all walking under random disturbance tasks, and HDPG achieves the best performance"(p.6 §VI-A1)。

**跨障碍(图6)。** "For 1.2cm obstacles, only HDPG achieves a success rate over 60%, while MHDDPG and DDPG 50% drop to about 50%"(p.6-7 §VI-A2);但能力有上限——1.4cm 时"although HDPG has the best performance, it is only 40%. This is due to the limitation of the height of the robot's leg lift"(p.7 §VI-A2),即便是最佳的 HDPG 成功率也只剩约 40%,作者归因于机器人抬腿高度受限。

**斜坡(图6)。** 此任务里多头本身帮助不大:"The performance of DDPG and MHDDPG is close, which means that multi-head critic doesn't have a strong effect in this task"(p.7 §VI-A3),但加上动态权重的 HDPG 仍最佳,12 度时成功率比其他两个算法高近 20%——"When the slope angle is 12 degrees, HDPG shows more robust performance, and the success rate is nearly 20% higher than the other two algorithms"(p.7 §VI-A3)。(总结者注:DDPG 与 MHDDPG 在斜坡上接近、而带动态权重的 HDPG 更优,可以解读为此任务的增益更多来自动态权重一侧;但原文只给了这两点观察,并未直接做"多头 vs 动态权重"的拆分归因,此为我的推断。)

**动态权重为什么 work(机制证据,图4 中/右)。** 没有动态权重的 MHDDPG"the step reward and gait reward are very unstable, which indicates that there is dependency between different reward components"(p.7 §VI-A4),而 HDPG 五个分量训练都稳定;更关键的是学习顺序印证了"先平衡后前进"的设计直觉:"the height reward and orientation reward first rises at about 2k episodes, while the gait reward and step reward then rise at about 6k episodes. This means that the robot first learns how to keep body balance, and then learns to walk forward"(p.7 §VI-A4)。

**Sim-to-real(图7)。** 仿真+动力学随机化训练的策略迁到实体机器人,受推后"the biped robot was disturbed by push force at 13.1s. Subsequently, The pelvis tilted at 13.5s... the robot recovers to stable gait at 13.9s"(p.7 §VI-B),约 0.8s 内恢复(单次演示)。

**MuJoCo 泛化(表I)。** "MHDDPG and HDPG outperform DDPG baselines on all tasks within 1M environment steps, and HDPG achieves the best performance on all tasks"(p.7-8 §VI-C)。把同样思路套到 PPO 得 HD-PPO:"HD-PPO has achieved a breakthrough of more than 25% on Hopper and Walker tasks, while it has nearly increased by about 6.6% on HalfCheetah task"(p.8 §VI-C)——作者据此说明奖励分量越多增益越大(HalfCheetah 只有 2 个分量,增益最小)。

## 核心贡献
1. **多头 critic + 混合策略梯度**:为每个奖励分量学独立价值函数并各自回传梯度,把"分解奖励"思路从离散 Q-learning(HRA/drQ)搬到连续控制 DDPG(supported,跨扰动/障碍/MuJoCo 多任务实验)。
2. **动态优先级权重**:用各分量奖励样本的均值/方差自动调梯度权重,让机器人按难度有序学习(实验观察到"先平衡后前进"且五分量稳定收敛;作者主张提升学习效率,observed→supported)。
3. **工程交付**:"We also release 3 challenges, which are push recovery challenges, obstacle challenges and slope terrain challenges"(p.1-2 §I),自建 AIDA 机器人并把仿真策略零额外调参迁移到实体(单机迁移演示,observed)。

## 局限与我的质疑
1. **多头与动态权重的功劳难以拆清,且增益并非处处显著。** 作者自己承认斜坡上"multi-head critic doesn't have a strong effect"(p.7);1.4cm 障碍上即便最佳的 HDPG 成功率也仅约 40%、且受抬腿高度物理上限制约(p.7)。这说明收益高度依赖任务,论文标题般的"reward-adaptive"通用性其实有边界。
2. **关键结论多为"图上读数"而非统计检验。** 抗扰动、障碍、斜坡的对比都来自成功率曲线图(图5/6),原文给的是"about 80%""over 60%""nearly 20% higher"这类近似描述;抗扰动任务做了 100 次试验(图5图注),但**未报告方差/置信区间或显著性**,而障碍/斜坡任务连试验次数都未在正文明示;唯一带标准差的是 MuJoCo 表I(10 次运行),且 **Walker2d 上三种方法的标准差都很大**(表I:HDPG 3217.5±1144.8、DDPG 2398.5±1169.7、MHDDPG 3162.0±1263.8,均超 ±1000;注:±528.1 其实是 HalfCheetah-v3 上 HDPG 的标准差,不是 Walker2d 的),"最佳"是否稳健存疑。因此除 MuJoCo 外的结果我只按 observed 看待,不宜读成"全面/大幅超越"。
3. **Sim-to-real 仅一次定性演示。** 13.1s 受扰、13.9s 恢复(图7)是单条视频时间线,没有迁移成功率统计、没有不同推力/方向的系统化实机评估,"successfully transferred"更接近概念验证而非定量结论。
4. **动态权重的"先易后难"是直觉驱动、缺反事实验证。** 式(9)用 μ_k(均值)代理"容易"、σ_k(方差)代理"不稳定",但论文没做"换一种权重函数/固定课程"的对照,无法排除是别的因素(如多头本身的梯度解耦)带来的稳定性;"先平衡后前进"只是从一条训练曲线推断的相关性,不是因果证明。
5. **对复用者的细节有缺口。** 给了主干超参(γ=0.99、lr、batch、T=20)与权重 w_1/w_3,但 w_2/w_4/w_5 笼统设 1.0、归一化所需的 d^g_max 等量、N(采样数)、探索噪声(OU)具体参数、动力学随机化范围等未在正文清楚给出;6 个分量奖励之间量纲/尺度差异是否被 max-min 充分对齐,也可能显著影响 μ_k/σ_k 的可比性,直接复现需补这些细节。