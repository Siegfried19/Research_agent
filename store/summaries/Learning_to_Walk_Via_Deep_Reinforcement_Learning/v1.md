---
paper_id: 10.15607/rss.2019.xv.011
version: 1
based_on: []
created_at: 2026-06-17
note: 首次总结
---

# Learning to Walk Via Deep Reinforcement Learning
> Tuomas Haarnoja, Sehoon Ha, Aurick Zhou, Jie Tan, George Tucker, Sergey Levine · 2019 ·  · 引用 434 · DOI 10.15607/rss.2019.xv.011

## 一句话
把 soft actor-critic 的温度参数从"手调超参"改成"对目标期望熵的自动梯度调节",从而得到一个样本高效、对超参不敏感的最大熵 RL 算法,并配上异步学习系统,在真实 Minitaur 四足机器人上**约两小时、约 400 个 rollout 从零学会行走**,无需任何仿真或动力学模型。

## 解决了什么问题
深度 RL 用于真实机器人运动的两大障碍是**样本复杂度高**和**对超参数敏感**:"applying deep RL to real-world robotic tasks is exceptionally difficult, primarily due to poor sample complexity and sensitivity to hyperparameters"(p.1 Abstract)。在物理腿式机器人上,反复试错调参既昂贵又会损坏硬件,因此多数前人工作退回到仿真里学习再迁移。最大熵 RL(SAC)虽样本高效且因熵最大化而鲁棒,但其**温度参数(熵权重)极其敏感、需逐任务手调**——这正是把它搬上真机的拦路虎。

## 用什么方法解决的
核心是**自动温度调节的 SAC**:不再手调温度,而是"employs gradient-based optimization of the temperature towards the targeted expected entropy over the visited states"(p.1 §I)。关键设计是**只约束对状态的期望熵,而非逐状态固定熵**——"our method controls only the expected entropy over the states, while the per-state entropy can still vary"(p.1 §I),使策略在确定性更优处自动降熵、在不确定处保持探索。

形式化为带最小期望熵约束的约束 MDP(公式2),取其拉格朗日松弛(公式3)并用对偶梯度法求解,其中"the dual variable takes the role of the temperature"(p.4 §V-A)——对偶变量即温度。温度更新沿(3)的负梯度方向:"We can optimize the temperature by moving it in the direction of the negative gradient of (3)"(公式7,p.4 §V-A);为保证非负,实际"parameterize"温度为 exp(β) 并优化 β(p.4 §V-B 脚注2)。整套对偶优化用截断近似——只"performs incomplete optimization"(p.4 §V-B),即对各目标交替各走一步而非优化到收敛。

实现上沿用标准 SAC 部件:高斯策略 + 两个 Q 函数("two parameterized Q-functions, with parameters",p.4 §V-B,公式8 用回归损失训练),策略用重参数化技巧最小化(公式9),温度用对偶目标(公式10)训练;"This procedure is the same as the standard soft actor-critic algorithm [17], but with an explicit, dynamic temperature"(p.4 §V-B)。算法 1 给出完整伪代码:每次迭代采样入 replay buffer,然后对 Q、策略、温度各做一步随机梯度,并用延迟目标网络。

工程上配一套**异步学习系统**,"consists of three components: a data collection job ..., a motion capture job that computes the reward signal ..., and a training job that updates the neural networks"(p.2 §III):机载电脑跑策略采数据,动捕系统测位置算奖励,工作站从 buffer 采样训练,三者异步运行,可单独暂停/重启以应对硬件与通信错误。

## 数据集 & 实验设置
**仿真基准**:四个 OpenAI Gym 连续运动任务,对比 SAC(逐环境调温度)、DDPG、PPO、TD3。所有算法同结构——"two-layer neural network with 256 hidden units on each layer, and we use ADAM [27] with the same learning rate of 0.0003"(p.5 §VI-A)。本方法目标熵设为每动作维 -1:"we simply set the target entropy to be -1 per action dimension (i.e., HalfCheetah has target entropy -6, while Humanoid uses -17)"(p.5 §VI-A)。

**真机 Minitaur**:八直驱电机四足。观测含 8 个电机角、横滚/俯仰角及其角速度,"We choose to exclude the yaw measurement because it drifts quickly"(p.7 §VII-A);动作为各腿摆角与伸展,经 PD 控制器跟踪。为安全用低增益 kp=0.3、kd=0.003;因延迟和部分观测导致非马尔可夫,"augment an observation space to include a history of the last five observations and actions which results in a 112 dimensional observation space"(p.7 §VII-A)。奖励鼓励前进距离、惩罚关节加速度/横滚角/前腿折叠,"The weights are set to 1.0, 0.05, 0.5, 1.0 respectively and the maximum angle threshold q is set to 0.3 radians"(p.7 §VII-A)。策略与价值网"fully connected feed-forward neural networks with two hidden-layers and 256 neurons per layer ... we smoothed out actions for the first 50 episodes"(p.7 §VII-A)。注意:**所有真机实验只在现实世界训练,不迁移任何仿真策略**。

## 主要结果 & 结论
**真机从零学会走(核心结果)**:"Our method successfully learns to walk from 160k control steps, or approximately 400 rollouts. Each rollout has the maximum length of 500 steps (equivalent to 10 seconds) and can terminate early if the robot falls"(p.7 §VII-B),整个过程约两小时。学到的步态"is periodic and synchronized, though no explicit trajectory generator, symmetry constraint, or periodicity constraint is encoded into the system"(p.7-8 §VII-B 图8)——周期性是涌现而非编码。

**鲁棒性与泛化(均为有限测试观察)**:对从未训练过的扰动,"it successfully recovered and returned to a periodic gait for all 10 repeated tests"(p.8 §VII-B 图8c);仅在平地训练却能上下坡、撞过木块、下台阶,"We repeated these tests for 10 times, and the robot succeeds on all cases"(p.8 §VII-B 图9)。

**仿真基准**:"Overall, our method performs better or comparably to the other baselines, standard SAC, DDPG, TD3, and PPO"(p.5-6 §VI-A1 图3)——注意原文措辞是"更好或相当",且其它算法都经过密集调参才得到这些曲线。

**敏感性(支持其核心主张)**:标准 SAC 对奖励尺度很敏感——"All benchmark environments achieve good performance for about the same range of values, between 1 to 10. On the other hand, the simulated Minitaur requires roughly two orders of magnitude larger reward scale to work properly"(p.5-6 §VI-A2 图4a),说明只在基准上调好不能迁移到貌似相似的新任务;而本方法"the range of good target entropy values is essentially the same for all environments"(p.6 §VI-A2 图4b),故同一套超参可跨任务复用。

**熵控制有效性**:"our algorithm is able to match the target entropy in a relatively small number of steps. On the other hand, regular SAC has a fixed temperature parameter and thus the entropy slowly decreases as the Q-function increases"(p.6 §VI-A3 图5)。

**仿真 Minitaur 数据效率与鲁棒机制**:本方法"is the most data efficient",而要拿到固定温度 SAC 的对照结果须扫候选温度选最优,"is equivalent to collecting an order of magnitude more samples"(p.6 §VI-B 图6b);机制上"our algorithm effectively escapes a local minimum of 'diving forward' ... by maintaining the policy entropy at higher values"(p.6 §VI-B 图6d)——即靠维持高熵跳出"向前扑倒"这一常见早停陷阱。鲁棒性方面,仿真机器人"can withstand up to 220N lateral pushes and subsequently recover to normal walking. This is significantly larger than the maximum 130N of the best PPO-trained policy that is picked out of 1000 learning trials"(p.6 §VI-B),作者推测鲁棒性源自训练时的熵最大化(注:这是 suspect/推测而非证明)。

## 核心贡献
1. **自动温度调节的最大熵 RL 算法**:把熵当约束、用对偶梯度自动调温度,几乎免去逐任务调参——"we propose a sample-efficient deep RL algorithm based on maximum entropy RL that requires minimal per-task tuning and only a modest number of trials to learn neural network policies"(p.1 Abstract)。
2. **端到端真机运动学习框架 + 异步系统**:"The principal contribution of our paper is an end-to-end RL framework for legged locomotion on physical robots, which includes a data efficient learning algorithm based on maximum entropy RL and an asynchronous learning system"(p.2 §I)。
3. **首次真机四足 + 无仿真预训练(作者自我定位)**:"To the best of our knowledge, our experiment is the first example of an application of deep reinforcement learning to quadrupedal locomotion directly on a robot in the real world without any pretraining in the simulation"(p.8 §VIII)。

## 局限与我的质疑
1. **作者自述局限**:当前系统两大未解难题——"the heavy dependency on manual resets between episodes and the lack of a safety layer that would enable learning on bigger robots"(p.8 §VIII)。即仍需人不断把摔倒/出界的机器人扶回来,且没有安全层,放不上 ANYmal/HyQ 这类大机器人。这意味着"全自动现实训练"的承诺在实践中仍高度依赖人工。
2. **真机结论建立在极小样本上**:鲁棒性与泛化的全部真机证据是**两组各 10 次重复测试**(扰动恢复、未见地形),且都是单一机器人、单一前进任务、0.32 m/s 的低速行走。这足以支撑"能 work、有一定鲁棒性"的 observed 级结论,但不足以支撑"普遍鲁棒/普遍泛化"。原文措辞克制(without difficulty / succeeds on all cases),复用者不应外推为强泛化保证。
3. **"对超参不敏感"主要在仿真上证明,真机只跑了一组配置**:敏感性分析(图4)是在仿真环境扫参得到的;真机侧只展示了一条用同套超参的成功学习曲线(图7),并没有在真机上做超参敏感性的对照实验。因此"真机免调参"更多是从仿真结论 + 一次成功的外推,而非真机上的直接证据。
4. **基准对比措辞被它自己限定为"相当或更好"**:图3 的卖点其实是"我没调参却追平了别人精调的结果",而非绝对刷分超越。若有人引用本文当作"SAC 自动温度版在基准上 SOTA 碾压",属于过度解读——原文只主张 better or comparably。另外只用了 5 个随机种子、阴影区取的是最好/最差种子,统计意义有限。
5. **关键鲁棒数字来自仿真而非真机**:220N vs 130N 这个最亮眼的对比是在**仿真 Minitaur** 上做的,且对 PPO 用的是"1000 次试次里挑最好",对比设置对基线不完全对等;而真机鲁棒性只有定性的推/恢复描述,没有力的量化。引用此数字时须注明是仿真结果。
6. **与本研究主题(训练稳定性/算法工具箱)的契合与不足**:本文对"减少调参负担、提升训练稳定性"贡献直接且重要(自动温度=SAC 的标配升级,即后来广为人知的 auto-α);但它对"训练过程中的诊断/监控/失败检测"几乎没有着墨,异步系统的稳定性、限速、风控等工程细节也只点到为止,想据此搭建可监控的真机训练流水线仍需大量补完。