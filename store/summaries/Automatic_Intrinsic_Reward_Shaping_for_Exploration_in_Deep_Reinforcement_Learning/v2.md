---
paper_id: 10.48550/arxiv.2301.10886
version: 2
based_on: []
created_at: 2026-06-17
note: 事实核查修正(Codex 抽检发现 4 处问题)
---

# Automatic Intrinsic Reward Shaping for Exploration in Deep Reinforcement Learning
> Mingqi Yuan, Bo Li, Xin Jin, Wenjun Zeng · 2023 · International Conference on Machine Learning · 引用 18 · DOI 10.48550/arxiv.2301.10886

## 一句话
把"用哪种内在奖励来促进探索"这件原本要人手挑、且会偏置目标的事,变成一个由 UCB 在线求解的多臂老虎机问题——AIRS 在训练的不同阶段,根据实时估计的**任务回报**自动从一个预定义内在奖励集合里挑当前最合适的塑形函数(包括"不塑形"),并附带一个 PyTorch 内在奖励工具箱。

## 解决了什么问题
内在奖励(curiosity / count-based / 熵最大化等)能促探索,但有两大痛点:① 同一种内在奖励在不同任务和不同学习阶段表现差异很大,"为某个任务选最好的内在奖励一直很难"("Selecting the best intrinsic reward for a specific task is always challenging",p.2);② 把内、外在奖励加在一起的联合目标是**有偏的**,最大化它只是有时才能得到外在奖励本身的最优策略,过度探索还可能拖垮已学到的策略("Since the joint objective composed of extrinsic and intrinsic rewards is biased, maximizing it only sometimes yields the optimal policy for the extrinsic reward alone … excessive exploration may lead to learning collapse",p.1–2)。AIRS 想让"用不用内在奖励、用哪个"这两个问题被**自动且实时**地回答——"AIRS selects shaping function from a predefined set based on the estimated task return in real-time, providing reliable exploration incentives and alleviating the biased objective problem"(p.1 Abstract)。

## 用什么方法解决的
把内在奖励选择形式化成多臂老虎机:"we formulate the intrinsic reward selection as a multi-armed bandit problem, in which different intrinsic reward functions are regarded as arms"(p.2),用上置信界(UCB)决策——"AIRS … uses the upper confidence bound (UCB) to make decisions based on the estimated task return"(p.1 图1)。关键巧思是用价值网络的**两个分支**分别估计"混合奖励 E+I"和"纯外在奖励 E"两套价值,再用后者(纯任务回报)作为评判每个内在奖励好坏的信号去更新各臂的 Q 值。此外作者放出一个工具箱:"we develop a toolkit that provides high-quality implementations of various intrinsic reward modules based on PyTorch"(p.2),即开源的 rllte。

---

## 动机 & 背景
论文先梳理三类内在奖励:count-based(按状态访问计数,如伪计数、RND)、curiosity-driven(用动力学预测误差,如 ICM、RIDE)、information-theory-based(状态熵/信息增益最大化,如 RE3、RISE)。它指出现有工作普遍存在"选择困难":同一内在奖励跨任务不稳定,且联合目标有偏。已有的 EIPO(Chen et al., 2022)虽用受约束的策略优化自动调内在奖励的重要性("automatically tunes the importance of the intrinsic reward via a principled constrained policy optimization procedure",p.2),能在不需要探索时抑制、需要时增大内在奖励,但被作者批评"too sophisticated to implement and cannot provide increments for arbitrary RL algorithms"(p.2)。AIRS 走的是另一条更轻的路:不调权重,而是**在一组候选里挑**。

## 方法细节
**带衰减权重的混合目标(Eq.1)。** 把内在奖励 I 以系数 β_t 加到外在奖励上最大化折扣回报,其中"β_t = β_0(1 − κ)^t is a weighting coefficient that controls the degree of exploration, and κ is a decay rate"(p.3 Eq.1)——即探索强度随训练衰减,早期多探索、后期收敛到纯任务奖励。

**底座 DAAC。** AIRS 在 Procgen 上搭在 Decoupled Advantage Actor-Critic(DAAC)之上,策略目标 J_DAAC(θ)=J_π+H_π−L_A(p.3 Eq.2):J_π 是 PPO 的策略梯度项、H_π 是鼓励探索的熵项、L_A=[A_θ(s_t,a_t)−Â_t]² 是优势回归损失;价值损失 L_V(φ)=[V_φ(s_t)−V̂_t]²(p.3 Eq.3)为均方误差。作者强调 DAAC 只是为泛化能力选的基准("we use DAAC as the benchmark due to its improvement in generalization ability. But AIRS can be combined with many RL algorithms",p.4),AIRS 本身可配多种 RL 算法。

**老虎机 + UCB(Eq.4)。** 选臂规则为 I_t = argmax[ Q_t(I) + c·√(log t / N_t(I)) ],其中"N_t(I) is the number of times that action I has been selected prior to time t, and c > 0 is a constant that controls the degree of exploration"(p.4 Eq.4)。

**任务回报作裁判(Eq.5、两分支价值网络)。** Q_t(I) 取一个长度为 W 的滑动窗口里平均估计回报的均值,"where ¯V^E_φ is the average estimated return of a rollout"(p.4 Eq.5)——注意裁判用的是**纯外在(任务)价值**,不是混合价值,这正是它声称能缓解偏置目标的机制:用谁(哪个内在奖励)更能把真实任务回报顶上去,就给谁更高的臂价值。为此"we employ two branches (Figure 2a) in the value network to estimate the state-value function evaluated by the mixed reward function E + I and extrinsic reward function E, respectively"(p.4),价值损失(Eq.6)同时回归 V^{E+I} 和 V^E 两支。

**候选集合。** 不同实验配不同池子。MiniGrid:"we build it on top of A2C+RE3, in which the intrinsic reward function pool only has ID and RE3"(p.5),ID 即 I_t=0(不塑形)。Procgen:"four intrinsic reward functions were selected to serve as the candidates, namely RE3, RISE, RIDE, and ID"(p.6),分别覆盖 Shannon 熵最大化、Rényi 熵最大化、显著状态变化、和不塑形(Table 1)。整个流程见 Algorithm 1(每轮 UCB 选一个 I→用它算内在奖励→更新策略与价值→把新策略的平均回报压入该臂的 FIFO 队列→重算 Q、计数+1)。

## 数据集 & 实验设置
三个基准:**MiniGrid**(Empty-16x16、DoorKey-6x6、DoorKey-8x8 三个稀疏奖励、难探索的网格世界,全可观测、7×7×3 紧凑编码,基线 A2C 与 A2C+RE3;附录 A.1)、**Procgen**(16 个程序生成游戏,15 个离散动作、64×64×3 RGB;在 200 个固定关卡上训、在完整关卡分布上测,考查泛化;基线 PPO/DrAC/DAAC/IDAAC/RE3/RISE/RIDE)、**DeepMind Control Suite**(Cartpole Balance、Cheetah Run、Finger Spin、Walker Walk 四个连续控制任务,且把背景换成合成/Kinetics 视频干扰来测泛化;基线 PPO、IDAAC;附录 C.1)。Procgen 全部"the best values are β_0 = 0.05, κ = 0.000025, c = 0.1, and W = 10"(p.15-16),训练 25M 帧 easy 模式;DMC 训练 1M 帧、每 10K 帧评估一次(附录 C.2)。结果用 rliable 报告 Median / IQM / Mean / Optimality Gap 四指标。MiniGrid 与 DMC 各任务**只报超参搜索后的最佳结果**("we only report the best results after a hyper-parameter search",p.6/p.9)。

## 主要结果 & 结论
- **MiniGrid:** "A2C+AIRS achieves the highest performance and convergence rate in all three games"(p.6),且 A2C+RE3 已能全面超过原始 A2C。(三个任务上观察到,样本量小。)
- **Procgen 总体:** "AIRS achieved the highest performance in all four metrics, especially for the Median and IQM"(p.7)。逐游戏看:训练集"AIRS achieves the highest performance in 8 out of 16 games, especially in the StarPilot game"(Table 5 caption),测试集"AIRS achieves the highest performance in 9 out of 16 games, especially in the BigFish game and StarPilot game"(Table 6 caption)。
- **相对强基线的优势是概率性的、不是压倒性的:** "the probability of AIRS being better than DAAC is 67%, and the probability of AIRS being better than IDAAC is 61%"(p.7 图6)。也就是说对当时最强的 DAAC/IDAAC,AIRS 只是略占上风。
- **决策行为(支撑"它真在按任务自适应选"):** "In BigFish game, the RIDE was the most selected function while AIRS chose no shaping in 33.7% of the training time. In Miner game, the RISE was the most selected function while AIRS chose no shaping in 59.3% of the training time"(p.7 图7),Ninja 中 RISE 最多、66.5% 选不塑形——不同游戏选的内在奖励和"不塑形"的占比差异很大,说明"不塑形"在很多游戏里扮演重要角色。
- **消融(候选集合越大越好的初步证据):** 把池子砍到只剩 RE3 与 ID 两个,"the test IQM and OG are 1.27 and 0.015, which was better than DAAC+RE3"(p.8)——即便最小池子也胜过固定用 RE3,但弱于完整四元池,佐证"自动从多个内在奖励里组装优势"的价值。
- **DMC:** "AIRS achieved the highest performance in all the tasks, while IDAAC outperformed the vanilla PPO agent in all the tasks"(p.9)。(四个任务上观察到。)

## 核心贡献
1. **把内在奖励选择形式化为多臂老虎机并用 UCB 在线求解**,候选臂含"不塑形"(ID),从而同时回答"用不用内在奖励 / 用哪个"。
2. **用纯外在(任务)回报作为臂的裁判信号**(价值网络双分支),以此缓解内外混合目标的偏置问题——这是它区别于"直接最大化混合目标"的机制核心。
3. **开源内在奖励工具箱 rllte**:"we develop a toolkit that provides high-quality implementations of various intrinsic reward modules based on PyTorch"(p.2);附录 D 的 Table 8 列了 PseudoCounts/ICM/RND/GIRM/NGU/RIDE/RE3/RISE/REVD 等模块的统一实现与公式,可即插即用("Each intrinsic reward module can be deployed in arbitrary algorithms in a plug-and-play manner")。
4. **在 MiniGrid / Procgen / DeepMind Control Suite 三类(离散+连续、含程序生成与背景干扰)基准上做了较全面验证**。

## 局限与我的质疑
1. **作者自述:依赖预定义池,池子质量直接决定上限。** "AIRS performs selection based on a pre-defined reward set. The quality of selected reward modules will inevitably affect the final policy performance"(p.9)。即 AIRS 只会"选",不会"造"——它无法超越候选集合里最好的那个在该任务上的潜力,如何构造一个好的奖励集合仍是开放问题。
2. **作者自述:奖励间可能相互干扰致学习崩溃 + 老虎机开销大、早期收敛慢。** "different intrinsic reward spaces may interfere with each other, resulting in unexpected learning collapse"(p.9);UCB 需"enough attempts are required to estimate the bound. This will significantly increase the computational overhead and decrease the convergence rate"(p.9),且实测"AIRS had a lower convergence rate in the early stage, which was mainly limited by the estimation accuracy of the value network"(p.7)。换句话说,它拿"前期更慢、算得更重"换"后期更稳更高"。
3. **(总结者注)优势幅度被标题口气放大。** 论文整体读起来像"全面超越",但对最强基线只有 67%/61% 的胜率(图6),且 16 个 Procgen 游戏只在 8(训练集)/9(测试集)个上拿第一——这是"略胜"而非"碾压"。复用者不应期待对任意任务都稳赢 DAAC/IDAAC。
4. **(总结者注)裁判信号本身可能不可靠,却是整个机制的命门。** Q 值靠"估计的任务回报"算,而作者自己承认价值估计精度是早期瓶颈;在稀疏/欺骗性奖励下,纯外在价值的估计方差很大,UCB 据此选臂可能噪声主导——论文没有给出"裁判信号有多准 / 选错臂的频率"这类直接证据,图7 只展示了选择分布,不能反推选得对不对。
5. **(总结者注)超参敏感性与"只报最佳"。** MiniGrid 和 DMC 明说"only report the best results after a hyper-parameter search",而 AIRS 引入了 β_0、κ、c、W 等额外超参;在没有大规模调参预算时能否复现这些增益、对超参多敏感,论文未充分回答。
6. **(总结者注)对本研究主题(reward 设计 / 稀疏奖励探索)的适配度。** 优点是直接给出"实时自动选内在奖励 + 工具箱",非常实战;但它假设你已有一组现成的内在奖励模块,且任务有可估计的(即便稀疏的)外在回报作裁判——对**完全无外在奖励**或奖励极度欺骗的场景,这套"用任务回报选臂"的机制是否还成立,论文未涉及,属于明确的未证明区。